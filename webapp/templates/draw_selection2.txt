SELECT v.id_candidato, SUM(v.votos) as votos 
FROM votos_candidato_establecimiento_paso_caba_2015 v, candidatos_paso_caba_2015 c 
WHERE v.id_establecimiento IN ({{ids}})
AND c.id_candidato = v.id_candidato
AND c.titular = 1
GROUP BY v.id_candidato
ORDER BY votos desc;